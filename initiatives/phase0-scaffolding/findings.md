# Phase 0: Scaffolding — Findings

- conftest.py should not be empty — needs at least a fixture stub
- Dockerfiles need at least a FROM line — empty files confuse agents
- pytest + pytest-asyncio must be in requirements.txt from the start
