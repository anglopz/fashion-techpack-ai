name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # Jobs to be defined in Phase 5
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "CI pipeline — to be configured in Phase 5"
